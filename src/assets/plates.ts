/**
 * Plate registry — one pair per SKU family.
 * Purchasable graphics resolve through KitSpec.plates → brandAssets PLATES.
 * Queen Bees crest must never appear here.
 */

export { PLATES, plateFor, type PlateProductId, type PlatePair } from "@/lib/brandAssets";
